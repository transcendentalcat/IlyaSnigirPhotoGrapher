@model IlyaSnigirPhotographer.Models.Photo

@{
    ViewBag.Title = "Display";
}

<h2>Display</h2>

<div>
    <h4>Photo</h4>
    @if (Model.PhotoFile != null)
    {
        <div class="photo-display">
            <img width="800" src="@Url.Action("GetImage", "Home", new { id = Model.PhotoID })" />
        </div>
    }
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Album.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Album.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ImageMimeType)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ImageMimeType)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CreatedDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CreatedDate)
        </dd>


    </dl>
</div>
@Html.ActionLink("Show comments", "GetPhotoComments", new { id = Model.PhotoID })

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.PhotoID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
